POSTGRES_DB=wishiz
POSTGRES_USER=wishiz
POSTGRES_PASSWORD=change-this-postgres-password
DATABASE_URL=postgres://wishiz:change-this-postgres-password@postgres:5432/wishiz?sslmode=disable

MINIO_ROOT_USER=wishiz
MINIO_ROOT_PASSWORD=change-this-minio-password
STORAGE_S3_BUCKET=wishiz-images
STORAGE_PUBLIC_BASE_URL=https://storage.your-domain.example

API_BIND_ADDR=0.0.0.0
MINIO_BIND_ADDR=127.0.0.1
MINIO_CONSOLE_BIND_ADDR=127.0.0.1

RUN_DB_MIGRATIONS=true
UPLOADS_ENABLED=true
SHARE_BASE_URL=https://wishiz.app

# Maintenance sweep of expired sessions/invites (optional — default 1h)
# CLEANUP_INTERVAL=1h

# Scraper (all optional — sensible defaults shown)
# SCRAPE_BUDGET=30s
# SCRAPE_RENDER_TIMEOUT=26s
# SCRAPE_MAX_CONCURRENT_RENDERS=3
# SCRAPE_SHOPIFY_PROBE=true
# SCRAPE_INFER_DOTCOM_USD=false
# SCRAPE_IMAGE_HEAD_CHECK=false
# SCRAPE_MAX_PRICE=10000000
# CHROMIUM_PATH=
